namespace Aynera.Domain.Photos.Exceptions;

public sealed class PhotoException : Exception
{
    public PhotoException(string errorCode, string message, int statusCode = 400)
        : base(message)
    {
        ErrorCode = errorCode;
        StatusCode = statusCode;
    }

    public string ErrorCode { get; }
    public int StatusCode { get; }
}
